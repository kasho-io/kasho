# Caddy reverse proxy for the Kasho demo.
#
# DEMO_HOST is injected via the .env file by the deploy workflow. Caddy
# provisions a Let's Encrypt cert for it automatically once DNS resolves
# to this box.

{$DEMO_HOST} {
	encode zstd gzip
	reverse_proxy demo:3001
}
