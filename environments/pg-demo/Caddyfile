# Caddy reverse proxy for the Kasho demo.
#
# Provisions a Let's Encrypt cert for demo.kasho.io automatically once the
# DNS A record points at this box.

demo.kasho.io {
	encode zstd gzip
	reverse_proxy demo:3001
}
