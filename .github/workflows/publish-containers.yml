name: Publish Container Images

on:
  workflow_dispatch:  # Allow manual triggering
  workflow_run:
    workflows: ["Test Suite"]
    types:
      - completed
    branches:
      - main
  push:
    tags:
      - 'v*'

env:
  IMAGE_NAME: kashoio/kasho
  DEMO_IMAGE_NAME: kashoio/kasho-demo

jobs:
  check-changes:
    # Run for workflow_run trigger, tag pushes, and manual dispatch
    if: ${{ github.event_name == 'workflow_dispatch' || (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success') || (github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')) }}
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
    outputs:
      should-build: ${{ steps.check.outputs.should-build }}
    steps:
      - name: Download change detection artifact
        id: download
        uses: actions/download-artifact@v4
        with:
          name: change-detection
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: ${{ github.event.workflow_run.id }}
        continue-on-error: true
        
      - name: Check if Docker build is needed
        id: check
        run: |
          # For tag pushes, always build
          if [[ "${{ github.event_name }}" == "push" && "${{ startsWith(github.ref, 'refs/tags/') }}" == "true" ]]; then
            echo "should-build=true" >> $GITHUB_OUTPUT
            echo "🏷️ Tag push detected, proceeding with build"
            exit 0
          fi
          
          # For workflow_run events, check the artifact
          echo "📊 Debug: Artifact download result: ${{ steps.download.outcome }}"
          echo "📊 Debug: Checking for change-detection.json file..."
          ls -la change-detection.json 2>/dev/null || echo "File not found"
          
          if [ -f change-detection.json ]; then
            echo "📊 Debug: Found change-detection.json, contents:"
            cat change-detection.json
            
            docker_changes=$(jq -r '.docker_changes' change-detection.json)
            echo "📊 Debug: docker_changes value: '$docker_changes'"
            
            if [ "$docker_changes" = "true" ]; then
              echo "should-build=true" >> $GITHUB_OUTPUT
              echo "✅ Docker-related changes detected, proceeding with build"
            else
              echo "should-build=false" >> $GITHUB_OUTPUT
              echo "⏭️ No Docker-related changes detected, skipping build"
            fi
          else
            # If no artifact, assume we should build (fail-safe)
            echo "should-build=true" >> $GITHUB_OUTPUT
            echo "⚠️ No change detection artifact found, proceeding with build"
          fi

  build-and-push:
    needs: [check-changes]
    if: ${{ needs.check-changes.result == 'success' && needs.check-changes.outputs.should-build == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      built-develop-image: ${{ steps.set-outputs.outputs.built-develop-image }}
    
    steps:
      - name: Debug job conditions
        run: |
          echo "📊 Debug: Event name: ${{ github.event_name }}"
          echo "📊 Debug: Is tag push: ${{ github.event_name == 'push' && startsWith(github.ref, 'refs/tags/') }}"
          echo "📊 Debug: Is workflow_run: ${{ github.event_name == 'workflow_run' }}"
          echo "📊 Debug: check-changes result: ${{ needs.check-changes.result }}"
          echo "📊 Debug: should-build output: ${{ needs.check-changes.outputs.should-build }}"
          echo "📊 Debug: Full condition result: ${{ (github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')) || (github.event_name == 'workflow_run' && needs.check-changes.result == 'success' && needs.check-changes.outputs.should-build == 'true') }}"
          
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            # For workflow_run or manual dispatch from main branch, tag as 'develop'
            type=raw,value=develop,enable=${{ github.event_name == 'workflow_run' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main') }}
            # For version tags, tag both with and without 'v' prefix
            type=semver,pattern={{version}}
            type=semver,pattern=v{{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern=v{{major}}.{{minor}}
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') }}
            # Also tag with the commit SHA for traceability
            type=sha,format=short

      - name: Build and push base image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.base
          platforms: linux/amd64,linux/arm64
          push: true
          tags: kashoio/kasho-base:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Get current date
        id: date
        run: echo "date=$(date -u +"%Y-%m-%dT%H:%M:%SZ")" >> $GITHUB_OUTPUT

      - name: Build and push production image
        uses: docker/build-push-action@v5
        with:
          context: .
          target: production
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            BASE_IMAGE=kashoio/kasho-base:latest
            LDFLAGS=-X kasho/pkg/version.Version=${{ steps.meta.outputs.version || 'develop' }} -X kasho/pkg/version.GitCommit=${{ github.sha }} -X kasho/pkg/version.BuildDate=${{ steps.date.outputs.date }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Extract demo image metadata
        id: demo-meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.DEMO_IMAGE_NAME }}
          tags: |
            type=raw,value=develop,enable=${{ github.event_name == 'workflow_run' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main') }}
            type=semver,pattern={{version}}
            type=semver,pattern=v{{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern=v{{major}}.{{minor}}
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') }}
            type=sha,format=short

      - name: Build and push demo image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./apps/demo/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.demo-meta.outputs.tags }}
          labels: ${{ steps.demo-meta.outputs.labels }}
          cache-from: type=gha,scope=demo
          cache-to: type=gha,mode=max,scope=demo

      - name: Save build status
        run: |
          echo "BUILT_DEVELOP_IMAGE=true" > build-status.txt

      - name: Upload build status artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-status
          path: build-status.txt
          retention-days: 1

  publish-status:
    runs-on: ubuntu-latest
    needs: [check-changes, build-and-push]
    if: always() && github.event_name == 'workflow_run'
    steps:
      - name: Container Publish Status
        run: |
          echo "📊 Container Publish Status Report"
          echo "================================="
          echo "Tests passed: ${{ github.event.workflow_run.conclusion == 'success' }}"
          echo "Docker changes detected: ${{ needs.check-changes.outputs.should-build }}"
          echo "Build job status: ${{ needs.build-and-push.result }}"
          
          if [[ "${{ github.event.workflow_run.conclusion }}" != "success" ]]; then
            echo "❌ Publish skipped: Tests failed"
          elif [[ "${{ needs.check-changes.outputs.should-build }}" != "true" ]]; then
            echo "⏭️ Publish skipped: No Docker-related changes detected"
          elif [[ "${{ needs.build-and-push.result }}" == "success" ]]; then
            echo "✅ Container images published successfully"
          else
            echo "❌ Container publish failed"
          fi